---
import Layout from '~/layouts/PageLayout.astro';
import { getCollection } from 'astro:content';

const posts = await getCollection('post');

const metadata = {
  title: 'Blog de Marketing Digital y SEO en Cádiz | AIRconsulting',
  description: 'Descubre las últimas tendencias en marketing digital, SEO y desarrollo web en Cádiz. Artículos especializados para empresas locales que quieren crecer online.',
  keywords: ['blog marketing digital', 'SEO Cádiz', 'desarrollo web Cádiz', 'tendencias digitales', 'AIRconsulting'],
};
---

<Layout metadata={metadata}>
  <section class="py-32 bg-black">
    <div class="max-w-7xl mx-auto px-4 sm:px-6">
      <div class="text-center mb-16">
        <h1 class="text-4xl sm:text-5xl md:text-6xl font-bold text-white mb-6">
          Blog de Marketing Digital en Cádiz
        </h1>
        <p class="text-xl text-gray-300 max-w-3xl mx-auto">
          Descubre las últimas tendencias, estrategias y consejos para hacer crecer tu negocio online en Cádiz y su provincia.
        </p>
      </div>

      <!-- Blog Posts Grid -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-16">
        {posts.map((post) => (
          <article class="bg-gray-900 rounded-lg overflow-hidden shadow-lg hover:shadow-xl transition-shadow duration-300">
            <a href={`/blog/${post.id}`} class="block">
              <div class="aspect-video overflow-hidden">
                <img
                  src={post.data.image}
                  alt={post.data.title}
                  class="w-full h-full object-cover hover:scale-105 transition-transform duration-300"
                />
              </div>
              <div class="p-6">
                <div class="flex items-center mb-3">
                  <span class="text-sm text-orange-500 font-medium uppercase tracking-wider">
                    {post.data.category}
                  </span>
                  <span class="mx-2 text-gray-600">•</span>
                  <time class="text-sm text-gray-400" datetime={post.data.publishDate?.toISOString()}>
                    {post.data.publishDate?.toLocaleDateString('es-ES', {
                      year: 'numeric',
                      month: 'long',
                      day: 'numeric'
                    })}
                  </time>
                </div>
                <h2 class="text-xl font-bold text-white mb-3 hover:text-orange-400 transition-colors">
                  {post.data.title}
                </h2>
                <p class="text-gray-300 text-sm leading-relaxed">
                  {post.data.excerpt}
                </p>
                <div class="mt-4 flex flex-wrap gap-2">
                  {post.data.tags?.slice(0, 3).map((tag) => (
                    <span class="text-xs bg-orange-500/20 text-orange-400 px-2 py-1 rounded-full">
                      #{tag}
                    </span>
                  ))}
                </div>
              </div>
            </a>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20 bg-gradient-to-r from-orange-500 to-orange-600">
    <div class="max-w-4xl mx-auto text-center px-4 sm:px-6">
      <h2 class="text-3xl sm:text-4xl font-bold text-white mb-6">
        ¿Necesitas Ayuda con tu Estrategia Digital?
      </h2>
      <p class="text-xl text-orange-100 mb-8">
        En AIR Consulting te ayudamos a implementar estas estrategias en tu negocio de Cádiz.
      </p>
      <a
        href="/#contacto"
        class="inline-block px-8 py-4 bg-white text-orange-600 font-bold rounded-full hover:bg-gray-100 transition-colors duration-200"
      >
        Solicitar Consultoría Gratuita
      </a>
    </div>
  </section>
</Layout>
